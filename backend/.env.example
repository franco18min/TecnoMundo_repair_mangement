# TecnoMundo Repair Management - Backend (.env ejemplo)

# --- Base de Datos ---
# Ajusta estas variables según tu instancia de PostgreSQL (local o gestionada)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=tecnomundo
DB_USER=postgres
DB_PASSWORD=cambia-esto

# SSL para DB (prefer|require). Si usas proveedores gestionados, probablemente require.
DB_SSLMODE=prefer

# Driver de PostgreSQL: pg8000 (recomendado en entornos sin compilación) o psycopg2
DB_DRIVER=pg8000

# --- CORS ---
# Lista separada por comas de orígenes permitidos
# Incluye tu dominio y subdominios que servirán el frontend
ALLOWED_ORIGINS=https://tecnoapp.ar,https://www.tecnoapp.ar,http://localhost:5173,http://localhost:5174

# --- Seguridad JWT ---
# Cambia esta clave en producción (usa al menos 32 caracteres aleatorios)
SECRET_KEY=cambia-esta-clave-en-produccion
JWT_ALGORITHM=HS256
# Tiempo de expiración del token (minutos)
ACCESS_TOKEN_EXPIRE_MINUTES=240

# --- App ---
# Puerto interno donde correrá Uvicorn/Gunicorn (CloudPanel hará reverse proxy)
APP_PORT=9001

# Nota: copia este archivo como backend/.env y edítalo para tu servidor.